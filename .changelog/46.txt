```release-note:breaking-change
variable/privatelink_byoe_regions: Changes type from `map(string)` to `map(object({ region, supported_remote_regions }))` to support cross-region BYOE PrivateLink
```

```release-note:enhancement
variable/privatelink_byoe: Adds optional `region` and `service_region_key` fields for cross-region BYOE. A VPC endpoint in one AWS region can reference an Atlas endpoint service in another via `service_region_key`
```
